<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Sweet</title>

  <!-- ✅ Bootstrap CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- ✅ Custom CSS -->
  <style>
    body {
      background-color: #fdf6f0;
      padding: 2rem;
    }
    .form-card {
      max-width: 600px;
      margin: auto;
      background-color: #fff;
      border-radius: 12px;
      padding: 2rem;
      box-shadow: 0 0 12px rgba(0,0,0,0.07);
    }
    .form-title {
      color: #d63384;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="form-card">
      <h2 class="form-title mb-4 text-center">✏️ Edit Sweet</h2>

      <form action="/api/sweets/editSweet/<%= sweet.id %>?_method=PUT" method="Post">
        <div class="mb-3">
          <label for="name" class="form-label">Sweet Name</label>
          <input type="text" class="form-control" id="name" name="name" value="<%= sweet.name %>" required>
        </div>

        <div class="mb-3">
          <label for="category" class="form-label">Category</label>
          <input type="text" class="form-control" id="category" name="category" value="<%= sweet.category %>" required>
        </div>

        <div class="mb-3">
          <label for="price" class="form-label">Price (₹)</label>
          <input type="number" class="form-control" id="price" name="price" value="<%= sweet.price %>" required>
        </div>

        <div class="mb-4">
          <label for="quantity" class="form-label">Quantity</label>
          <input type="number" class="form-control" id="quantity" name="quantity" value="<%= sweet.quantity %>" required>
        </div>

        <div class="d-flex justify-content-between">
          <a href="/" class="btn btn-secondary">⬅ Delete</a>
          <button type="submit" class="btn btn-primary">💾 Save Changes</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
