<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Sweet Shop</title>

  <!-- ✅ Bootstrap CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- ✅ Custom CSS -->
  <style>
    body {
      background-color: #fdf6f0;
      padding: 2rem;
    }
    .sweet-card {
      border: 1px solid #eee;
      border-radius: 12px;
      padding: 1.2rem;
      margin-bottom: 1rem;
      box-shadow: 0 0 8px rgba(0,0,0,0.05);
    }
    .sweet-name {
      font-size: 1.2rem;
      font-weight: 600;
      color: #d63384;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-4 text-center text-primary">🍬 Sweet Shop </h1>

    <div class="d-flex justify-content-end mb-4">
      <a href="/api/sweets/add" class="btn btn-success">➕ Add New Sweet</a>
    </div>

    <% if (sweets.length === 0) { %>
      <p class="text-muted">No sweets available right now.</p>
    <% } else { %>
      <div class="row">
        <% sweets.forEach(sweet => { %>
          <div class="col-md-4">
            <div class="sweet-card bg-white">
              <div class="sweet-name"><%= sweet.name %></div>
              <p>Category: <%= sweet.category %></p>
              <p>Price: ₹<%= sweet.price %></p>
              <p>Quantity: <%= sweet.quantity %></p>
              <div class="d-flex justify-content-between">
                <a href="/api/sweets/edit/<%= sweet._id %>" class="btn btn-sm btn-outline-primary">✏️ Edit</a>
                <a href="/api/sweets/purchase/<%= sweet.id %>" class="btn btn-sm btn-outline-success">🛒 Purchase</a>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>
</body>
</html>
