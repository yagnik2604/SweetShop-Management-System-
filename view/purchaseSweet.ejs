<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Purchase Sweet</title>

  <!-- ✅ Bootstrap CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- ✅ Custom CSS -->
  <style>
    body {
      background-color: #fdf6f0;
      padding: 2rem;
    }
    .form-card {
      max-width: 600px;
      margin: auto;
      background-color: #fff;
      border-radius: 12px;
      padding: 2rem;
      box-shadow: 0 0 12px rgba(0,0,0,0.07);
    }
    .form-title {
      color: #0d6efd;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="form-card">
      <h2 class="form-title mb-4 text-center">🛒 Purchase <%= sweet.name %></h2>

      <form action="/api/sweets/purchase/<%= sweet.id %>?_method=PATCH" method="POST">
        <div class="mb-3">
          <label for="amount" class="form-label">Quantity to Purchase</label>
          <input type="number" class="form-control" id="amount" name="amount" min="1" max="<%= sweet.quantity %>" required>
          <div class="form-text text-muted">Available: <%= sweet.quantity %></div>
        </div>

        <div class="d-flex justify-content-between">
          <a href="/" class="btn btn-secondary">⬅ Back</a>
          <button type="submit" class="btn btn-primary">✅ Purchase</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
